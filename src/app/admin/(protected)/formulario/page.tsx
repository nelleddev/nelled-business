import { AdminPlaceholder } from "@/components/admin/admin-placeholder";

export default function ContactFormPage() {
  return (
    <AdminPlaceholder
      title="Formulário"
      description="Personalize título, descrição, campos, placeholders, botão e mensagem enviada para o WhatsApp."
    />
  );
}
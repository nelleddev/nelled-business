import { AdminPlaceholder } from "@/components/admin/admin-placeholder";

export default function DomainPage() {
  return (
    <AdminPlaceholder
      title="Domínio"
      description="Gerencie o endereço Nelled e conecte um domínio personalizado à empresa."
    />
  );
}